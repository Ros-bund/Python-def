# объявление функции
def get_factors(num):
    f = []
    for i in range(1, num + 1):
        if num % i == 0:
            f.append(i)
    return f

# считываем данные
n = int(input())

# вызываем функцию
print(get_factors(n))